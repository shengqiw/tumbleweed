import { StyleSheet } from 'react-native';

export const styles: any = StyleSheet.create({
    mapContainer: {
      flex: 1,
      width: '100%',
      alignItems: 'center',
      justifyContent: 'center',
    }
  });
  